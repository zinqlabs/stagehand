{
  "name": "stagehand",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "example": "pnpm build-dom-scripts && tsx examples/index.ts",
    "format": "prettier --write .",
    "cache:clear": "rm -rf .cache",
    "evals": "pnpm build-dom-scripts && npx braintrust eval",
    "build-dom-scripts": "mkdir -p ./lib/dom/build && mkdir -p ./dist/dom/build && esbuild ./lib/dom/*.ts --bundle --outdir=./lib/dom/build && cp ./lib/dom/build/* ./dist/dom/build",
    "build": "node build.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "esbuild": "^0.21.4",
    "prettier": "^3.2.5",
    "tsx": "^4.10.5",
    "braintrust": "^0.0.127",
    "autoevals": "^0.0.64"
  },
  "peerDependencies": {
    "@playwright/test": "^1.42.1",
    "deepmerge": "^4.3.1",
    "dotenv": "^16.4.5",
    "openai": "^4.29.2",
    "zod": "^3.23.8"
  },
  "dependencies": {
    "@instructor-ai/instructor": "^1.3.0"
  },

  "pnpm": {
    "patchedDependencies": {
      "@instructor-ai/instructor@1.3.0": "patches/@instructor-ai__instructor@1.3.0.patch"
    }
  }
}
